describe('type data primitif', function ():void {
    it('harus di deklarasi',function():void {
        const name: string = "lalu doni setiawan";
        const balance: number = 12345678;
        const isVip: boolean = true;

        console.info(name);
        console.info(balance);
        console.info(isVip);

    });
})